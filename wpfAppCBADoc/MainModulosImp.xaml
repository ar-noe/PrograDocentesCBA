<Window x:Class="wpfAppCBADoc.MainModulosImp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfAppCBADoc"
        mc:Ignorable="d"
       
        Title="Sistema CBA - Gestión de Módulos Impartidos" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Menú Lateral -->
            <Border Grid.Column="0" Background="{StaticResource PrimaryBlueBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Logo o Título -->
                    <Border Grid.Row="0" Background="{StaticResource DarkBlueBrush}" 
                        Height="100" Padding="20">
                        <Image x:Name="logo" 
                       Source="/images/cba.png"
                       Stretch="Uniform"
                       Height="80"
                       Width="120"/>
                    </Border>

                    <!-- Opciones del Menú -->
                    <StackPanel Grid.Row="1" Margin="10,20">
                    <Button x:Name="btnHorarios" Content="📅 Schedules" 
                            Style="{StaticResource MenuButtonStyle}"
                            Margin="0,2" Height="45"
                            Click="BtnHorarios_Click"/>

                    <Button x:Name="btnModulos" Content="📚 Imparted Modules" 
                            Style="{StaticResource MenuButtonStyle}"
                            Margin="0,2" Height="45"
                            Background="{StaticResource LightBlueBrush}"
                            Foreground="{StaticResource DarkTextBrush}"/>

                        <Button x:Name="btnAulas" Content="🏫 Classroom Manager" 
                            Style="{StaticResource MenuButtonStyle}"
                            Margin="0,2" Height="45"
                            Click="BtnAulas_Click"/>
                    </StackPanel>

                    <!-- Footer -->
                <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
                    <Button Content="➜] Exit"  x:Name="btnExit"
        Style="{StaticResource SecondaryButtonStyle}"
            
        Margin="0,0,0,30"
        Width="150" Click="Button_Click"/>
                    <Border Grid.Row="2" Background="{StaticResource DarkBlueBrush}">
                        <StackPanel VerticalAlignment="Bottom"  Margin="10">
                            <!-- Texto del sistema abajo -->
                            <TextBlock Text="CBA Management System v1.0" 
                        Foreground="{StaticResource WhiteBrush}"
                        HorizontalAlignment="Center"
                        FontSize="10"
                        TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
            </Border>

            <!-- Área de Contenido Principal -->
            <Border Grid.Column="1" Background="{StaticResource BackgroundBrush}" 
                Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Título Principal -->
                    <Border Grid.Row="0" Background="{StaticResource WhiteBrush}" 
                        CornerRadius="8" Padding="20,15" Margin="0,0,0,15"
                        BorderBrush="{StaticResource LightBlueBrush}" 
                        BorderThickness="2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Imparted Modules Management" 
                                  Foreground="{StaticResource DarkTextBrush}"
                                  FontSize="24" FontWeight="Bold"
                                  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Contenido Principal -->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Formulario para agregar módulos -->
                        <GroupBox Grid.Row="0" Header="Add a new Imparted Module" 
                              Style="{StaticResource GroupBoxStyle}">
                        <!-- Dentro del GroupBox "Add a new Imparted Module" -->
                        <Grid Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Fila 1: Curso -->
                            <Label Grid.Row="0" Grid.Column="0" Content="Course:" 
           Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox Grid.Row="1" Grid.Column="0" 
              Style="{StaticResource ComboBoxStyle}"
              x:Name="cmbCurso"
              Margin="0,0,5,15"
              SelectionChanged="CmbCurso_SelectionChanged"/>

                            <!-- Fila 1: Módulo (ahora en columna 1) -->
                            <Label Grid.Row="0" Grid.Column="1" Content="Module:" 
           Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" 
              Style="{StaticResource ComboBoxStyle}"
              x:Name="cmbModulo"
              Margin="5,0,5,15"
              IsEnabled="False"/>
                            <!-- Deshabilitado hasta que se seleccione curso -->

                            <!-- Fila 1: Sucursal (ahora en columna 3) -->
                            <Label Grid.Row="0" Grid.Column="3" Content="Branch:" 
           Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox Grid.Row="1" Grid.Column="3" 
              Style="{StaticResource ComboBoxStyle}"
              x:Name="cmbSucursal"
              Margin="5,0,5,15"
              SelectionChanged="CmbSucursal_SelectionChanged"/>

                            <!-- Fila 1: Aula (ahora en columna 4) -->
                            <Label Grid.Row="0" Grid.Column="4" Content="Classroom:" 
           Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox Grid.Row="1" Grid.Column="4" 
              Style="{StaticResource ComboBoxStyle}"
              x:Name="cmbAula"
              Margin="5,0,0,15"
              IsEnabled="False"/>
                            <!-- Deshabilitado hasta que se seleccione sucursal -->

                            <!-- Fila 2: Bimestre (nueva fila) -->
                            <Label Grid.Row="2" Grid.Column="0" Content="Bimester:" 
           Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox Grid.Row="3" Grid.Column="0" 
              Style="{StaticResource ComboBoxStyle}"
              x:Name="cmbBimestre"
              Margin="0,0,5,0"/>

                            <!-- Botones (ahora en fila 3, columnas 3-4) -->
                            <StackPanel Grid.Row="3" Grid.Column="3" Grid.ColumnSpan="2" 
                Orientation="Horizontal" 
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom">
                                <Button Content="➕ Add Module" 
                Style="{StaticResource PrimaryButtonStyle}"
                Click="BtnAgregarModulo_Click"
                Margin="0,0,10,0"
                Width="160"/>
                                <Button Content="🔄 Clear" 
                Style="{StaticResource SecondaryButtonStyle}"
                Click="BtnLimpiarFormulario_Click"
                Width="100"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>

                        <!-- Lista de módulos existentes -->
                        <GroupBox Grid.Row="1" Header="Existing Imparted Modules" 
                              Style="{StaticResource GroupBoxStyle}" Margin="0,15,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Barra de búsqueda -->
                                <Border Grid.Row="0" Background="{StaticResource LightGrayBrush}" 
                                    CornerRadius="4" Padding="10" Margin="0,0,0,10">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox x:Name="txtBuscarModulo" 
                                             Style="{StaticResource TextBoxStyle}" 
                                             Width="300" Margin="0,0,10,0"
                                             TextChanged="TxtBuscarModulo_TextChanged"
                                             Background="{StaticResource WhiteBrush}"/>
                                        <Button Content="🔍 Search" 
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Click="BtnBuscarModulo_Click"
                                            Padding="15,8"/>
                                        <Button Content="🔄 Update" 
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Click="BtnActualizar_Click"
                                            Padding="15,8"
                                            Margin="10,0,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- DataGrid para mostrar módulos -->
                                <DataGrid Grid.Row="1" x:Name="dgModulos" 
                                      Style="{StaticResource DataGridStyle}"
                                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="ID -(?)" Binding="{Binding IdModuloImp}" Width="60"/>
                                        <DataGridTextColumn Header="Professor" Binding="{Binding Docente}" Width="200"/>
                                        <DataGridTextColumn Header="Module" Binding="{Binding Modulo}" Width="180"/>
                                        <DataGridTextColumn Header="Course" Binding="{Binding Curso}" Width="150"/>
                                        <DataGridTextColumn Header="Classroom" Binding="{Binding Aula}" Width="100"/>
                                        <DataGridTextColumn Header="Branch" Binding="{Binding Sucursal}" Width="120"/>
                                        <DataGridTextColumn Header="Bimester" Binding="{Binding Bimestre}" Width="100"/>
                                        <DataGridTextColumn Header="Management" Binding="{Binding Gestion}" Width="80"/>
                                        <DataGridTemplateColumn Header="Acciones" Width="120">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Content="✏️" 
                                                            ToolTip="Edit"
                                                            Style="{StaticResource PrimaryButtonStyle}"
                                                            Padding="8" Margin="2"
                                                            Click="BtnEditarModulo_Click"
                                                            Width="35" Height="30"/>
                                                        <Button Content="🗑️" 
                                                            ToolTip="Delete"
                                                            Style="{StaticResource SecondaryButtonStyle}"
                                                            Padding="8" Margin="2"
                                                            Click="BtnEliminarModulo_Click"
                                                            Width="35" Height="30"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Window>