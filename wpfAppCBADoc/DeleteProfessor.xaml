<Window x:Class="wpfAppCBADoc.DeleteProfessor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfAppCBADoc"
        mc:Ignorable="d"
        Title="Sistema CBA - Gestión de Módulos Impartidos" 
    Height="700" Width="1200"
    WindowStartupLocation="CenterScreen"
    Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Menú Lateral -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryBlueBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo o Título -->
                <Border Grid.Row="0" Background="{StaticResource DarkBlueBrush}" 
                    Height="100" Padding="20">
                    <Image x:Name="logo" 
                   Source="/images/cba.png"
                   Stretch="Uniform"
                   Height="80"
                   Width="120"/>
                </Border>

                <!-- Opciones del Menú -->
                <StackPanel Grid.Row="1" Margin="10,20">
                    <Button x:Name="btnHorarios" Content="📅 Schedules" 
                        Style="{StaticResource MenuButtonStyle}"
                        Margin="0,2" Height="45"
                        Click="BtnHorarios_Click"/>

                    <Button x:Name="btnModulos" Content="📚 Imparted Modules" 
                        Style="{StaticResource MenuButtonStyle}"
                        Margin="0,2" Height="45" Click="btnModulos_Click"
                        />

                    <Button x:Name="btnAulas" Content="🏫 Classroom Manager" 
                        Style="{StaticResource MenuButtonStyle}"
                        Margin="0,2" Height="45"
                        Click="BtnAulas_Click"/>
                    <Button x:Name="btnDeleteProfessor" Content="👔 Delete Professor" 
                            Style="{StaticResource MenuButtonStyle}"
                            Background="{StaticResource LightBlueBrush}"
                            Foreground="{StaticResource DarkTextBrush}"
                        Margin="0,2" Height="45"
                        />
                </StackPanel>

                <!-- Footer -->
                <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
                    <Button Content="➜] Exit"  x:Name="btnExit"
    Style="{StaticResource SecondaryButtonStyle}"

    Margin="0,0,0,30"
    Width="150"/>
                    <Border Grid.Row="2" Background="{StaticResource DarkBlueBrush}">
                        <StackPanel VerticalAlignment="Bottom"  Margin="10">
                            <!-- Texto del sistema abajo -->
                            <TextBlock Text="CBA Management System v1.0" 
                    Foreground="{StaticResource WhiteBrush}"
                    HorizontalAlignment="Center"
                    FontSize="10"
                    TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Área de Contenido Principal -->
        <Border Grid.Column="1" Background="{StaticResource BackgroundBrush}" 
            Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Título Principal -->
                <Border Grid.Row="0" Background="{StaticResource WhiteBrush}" 
                    CornerRadius="8" Padding="20,15" Margin="0,0,0,15"
                    BorderBrush="{StaticResource LightBlueBrush}" 
                    BorderThickness="2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Imparted Modules Management" 
                              Foreground="{StaticResource DarkTextBrush}"
                              FontSize="24" FontWeight="Bold"
                              VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Contenido Principal -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Formulario para agregar módulos -->


                    <!-- Lista de módulos existentes -->
                    <GroupBox Grid.Row="1" Header="Existing Imparted Modules" 
                          Style="{StaticResource GroupBoxStyle}" Margin="0,15,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Barra de búsqueda -->
                            <Border Grid.Row="0" Background="{StaticResource LightGrayBrush}" 
                                CornerRadius="4" Padding="10" Margin="0,0,0,10">
                                <StackPanel Orientation="Horizontal">
                                    <TextBox x:Name="txtSearchProfessor" 
                                         Style="{StaticResource TextBoxStyle}" 
                                         Width="300" Margin="0,0,10,0"
                                         Background="{StaticResource WhiteBrush}"/>
                                    <Button x:Name="btnDeleteSearch"
                                        Content="🔍 Search" 
                                        Style="{StaticResource PrimaryButtonStyle}"

                                        Padding="15,8" Click="btnDeleteSearch_Click"/>

                                </StackPanel>
                            </Border>

                            <!-- DataGrid para mostrar módulos -->
                            <DataGrid Grid.Row="1" x:Name="dgProfessors" 
                                  Style="{StaticResource DataGridStyle}"
                                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID Card" Binding="{Binding CI}" Width="150"/>
                                    <DataGridTextColumn Header="Professor's Name" Binding="{Binding Nombres}" Width="200"/>
                                    <DataGridTextColumn Header="Last Name" Binding="{Binding ApPat}" Width="180"/>

                                    <DataGridTemplateColumn Header="" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="❌" 
                                                        ToolTip="Delete"
                                                        Style="{StaticResource PrimaryButtonStyle}"
                                                        Padding="8" Margin="2"
                                                        Click="BtnDeleteProf_Click"
                                                        Width="35" Height="30"/>

                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </GroupBox>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>