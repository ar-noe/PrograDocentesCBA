<Window x:Class="wpfAppCBADoc.Schedules"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpfAppCBADoc"
        mc:Ignorable="d"
                Title="Sistema CBA - Gestión de Módulos Impartidos" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menú Lateral -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryBlueBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="100"/>

                </Grid.RowDefinitions>

                <!-- Logo o Título -->
                <Border Grid.Row="0" Background="{StaticResource DarkBlueBrush}" 
                    Height="100" Padding="20">
                    <Image x:Name="logo" 
                   Source="/images/cba.png"
                   Stretch="Uniform"
                   Height="80"
                   Width="120"/>
                </Border>

                <!-- Opciones del Menú -->
                <StackPanel Grid.Row="1" Margin="10,20">
                    
                    <Button x:Name="btnHorarios" Content="📅 Schedules" 
                        Style="{StaticResource MenuButtonStyle}"
                        Background="{StaticResource LightBlueBrush}"
                        Foreground="{StaticResource DarkTextBrush}"
                        Margin="0,2" Height="45"/>
                </StackPanel>

                <!-- Footer -->
                <StackPanel x:Name="btnExit" Grid.Row="2" VerticalAlignment="Center">
                    <Button Content="➜] Exit" 
                        Style="{StaticResource SecondaryButtonStyle}"
                            
                        Margin="0,0,0,0"
                        Width="150" Click="Button_Click"/>
                </StackPanel>

            </Grid>
        </Border>

        <!-- Área de Contenido Principal -->
        <Border Grid.Column="1" Background="{StaticResource BackgroundBrush}" 
            Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Título Principal -->
                <Border Grid.Row="0" Background="{StaticResource WhiteBrush}" 
                    CornerRadius="8" Padding="20,15" Margin="0,0,0,15"
                    BorderBrush="{StaticResource LightBlueBrush}" 
                    BorderThickness="2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Assigned Module" 
                              Foreground="{StaticResource DarkTextBrush}"
                              FontSize="24" FontWeight="Bold"
                              VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Contenido Principal -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Formulario para agregar módulos -->
                    <GroupBox Grid.Row="1" Header="Professor's Schedule" 
      Style="{StaticResource GroupBoxStyle}" Margin="0,15,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="50"/>
                            </Grid.RowDefinitions>


                            <!-- DataGrid para mostrar módulos -->
                            <DataGrid Grid.Row="1" x:Name="dgModulos" 
              Style="{StaticResource DataGridStyle}"
              ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding IdModuloImp}" Width="60"/>
                                    <DataGridTextColumn Header="Schedule" Binding="{Binding Docente}" Width="200"/>
                                    <DataGridTextColumn Header="Module" Binding="{Binding Modulo}" Width="180"/>
                                   
                                    
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- Botones -->
                            <StackPanel x:Name="btnMoreInfo"
                                Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
            Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="➕ Info" 
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Margin="0,10,70,0"
                                    Width="100" />

                            </StackPanel>
                        </Grid>
                    </GroupBox>

                    <!-- Lista de módulos existentes -->
                    
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
